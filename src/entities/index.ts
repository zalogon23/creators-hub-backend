import { Reaction } from "./reaction.entity";
import { User } from "./user.entity";
import { Video } from "./video.entity";

const entities = [
    User, Video, Reaction
]

export { User, Video, Reaction }

export default entities;